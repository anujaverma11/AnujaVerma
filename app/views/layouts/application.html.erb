<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Anuja Verma Portfolio" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Anuja Verma Portfolio" %>">
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="animate.css">
    <link rel="stylesheet" href="assets/animate.css">
    <link href='https://fonts.googleapis.com/css?family=Pinyon+Script|Ropa+Sans|Droid+Sans+Mono|Armata|Special+Elite' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
  </body>
</html>
